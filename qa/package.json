{
  "name": "maneno-qa",
  "version": "1.0.0",
  "private": true,
  "description": "E2E launch-regression harness for the Maneno PWA (dev-only; not shipped).",
  "scripts": { "test:e2e": "playwright test" },
  "devDependencies": { "@playwright/test": "^1.49.0" }
}
